## A. The prompt is incomplete: it references a section that was not assembled

**A1. §12 is cited 15+ times and does not exist in this prompt.** Locations: §7.1 ("`/solve` redirects to `/placement` per §12"), §7.3.6 item 3, §7.10 ("all eight `RefusalCode` values, including the three added in §12"), §9.1 (`apps/placement/`, `packages/placement/`), §9.2, §9.3 (`no-placement-leak` rule), §9.4.2 (`NoConsentRecord`/`NoPreferenceCoverage`/`NoLegalBasis` "§12 extends the union"), §9.9 (`pnpm build` excludes `apps/placement`), §9.13 (`grep -r "highs" apps/web/dist/`), §9.16 (`GOVERNANCE.md` + `compliance/ai-act/*.md` "§12-owned"), §11.8 R18. The file exists on disk at `/home/user/exodus/docs/prompt/section-12-placement-and-solidarity.md` (289 lines) and was left out of the assembly. **Fix: append §12 verbatim to the prompt, or strip every reference to it.** Shipping as-is, the agent has a `dependency-cruiser` rule, a build exclusion, a CI grep and three enum members for a package whose spec it was never given.

**A2. §12 and §1/§2/§5/§10 give flatly opposite orders and the prompt contains no resolution.** §1.8 N1 "Never in-box"; §1.8 N17 person-shaped types "Never"; §2 D-45 "the allocation MILP is cut entirely"; §5.10 "Do not import `highs`… Do not add a `solve()` stub, a `Model` implementation, or a type named `Assignment`, `Placement`"; §10.6 "You MUST NOT create `/compliance/ai-act/`". §12.0 reverses all of it ("That decision is overridden by the product owner, deliberately and on the record") and §12.11 mandates `/compliance/ai-act/{risk-management,…}.md`. §11.8 R18 gestures at a resolution but is circular — it says a later section "may not modify any M0–M8 acceptance criterion", while §12 modifies M7.4 (`/solve` target), §7.10 (eight refusal codes) and §10.1 E1 (person-shaped types banned by `check:policy` over `packages/`, which would fail the build on `packages/placement/schema/Case`). **Fix: in §1.8, replace N1/N17 with the amended form — "N1: no allocation solver in `packages/{contracts,kernel,semantic,globe,ui}` or the six public routes; the solver ships only as `@exodus/placement` per §12" — and add to §10.1 E1 the path exclusion `packages/placement/**` with the exclusion itself unit-tested, exactly as §1.6 step 4 does for `docs/research/**`.**

**A3. `REFUSAL_CODES` is closed at five in §5.1 ("You do not add a sixth") and at eight in §9.4.2/§7.10/§7.14.7.** §5.11's sweep test exercises five. The three §12 codes have no `reason` strings, no producer, and no test in §5.11 or §10.13. **Fix: one declaration, in §5.1, of the eight-member array with the three §12 reason strings quoted verbatim, and a line in §5.11 saying which of the eight are exercised by the core sweep and which by the placement suite.**

**A4. `pnpm check:policy`'s banned-identifier rule is seeded from `NON_GOALS[].bannedIdentifiers`, which §1.6 seeds with `['highs','lp-model','glpk','solvePlacement','commitPlacement']`.** §12.2 requires `commitPlacement()` to exist and §12 requires HiGHS. The linter fails the build on the module the same prompt orders built. **Fix: state in §1.6 that the banned-identifier walk excludes `packages/placement/**` and `apps/placement/**`, or remove those five identifiers from `NON_GOALS`.**

## B. The single largest silent gap: no table has a schema

**B1. Zero of the ~15 Tier A tables has a column list, and `TableEntry.columns` is a mandatory manifest field with `.min(1)`.** §3.3 gives paths and one-line descriptions ("`flows.parquet` gaskin-abel spine, 12k corridors x 34 years") and nothing else. The agent cannot write `snapshot:build`, cannot write `manifest.tables[].columns`, cannot validate against `SnapshotManifestSchema`, and cannot know whether `flows.parquet` carries `stock_mean`/`stock_std` (the cut ladder's step 4 removes them, implying they exist; the description never lists them) or `radiation_pred`/`s_ij` (§5.6.3 says ship them "as two derived columns on the corridor table"). **Fix: add a §3.3a table, one row per file, with `{column, arrowType, UnitId, nullable}` for every column — this is ~15 short tables and it is the interface every other section reads.** Without it the agent invents 15 schemas, which is exactly the failure §3's rule 1 forbids.

**B2. `flows.parquet` is described as "12k corridors × 34 years ≈ 408k rows" and also as the source of `AttributeCube.frames.width`, `.dashArray` and `.color` — but nothing says whether `d_p` (6 period values) and `C` (per country) are materialised into it, joined at load, or computed in the browser.** §6.5 `buildAttributeCube(facts, manifest)` takes `Fact[]`; §6.4 says corridors come from `snapshot/corridors.parquet` — a file that appears nowhere in §3.3 or §9.1. **Fix: name one file, state that `d_p` is expanded to 34 annual values at build time (with the period-step invariant asserted in the builder, not the renderer), and state that `C` is joined per corridor at build time.**

**B3. Three mutually exclusive snapshot file lists.** §3.3: `flows, flows_period, mirror, asylum, wpp_pop, wpp_life, wpp_diff, capacity, dyads, h3_pop, incidents, xwalk, centroids, geo/adm0.geojson.gz, geo/disputed.geojson.gz`. §9.1: `flows, stocks, disagreement, places, capacity, demography, dyads, h3_r3, mirror, asylum_monthly, incidents, admin0.geojson.gz, graticule.geojson.gz, centroids, xwalk`. §6.4: `adm0.geojson.gz`, `h3_r3.parquet`, `corridors.parquet`, `corpus-index.json`. Seven names appear in exactly one list. **Fix: delete the §9.1 and §6.4 lists and make §3.3 normative by reference.**

**B4. `Fact` has three incompatible definitions in the same package.** §3.8 `packages/contracts/src/fact.ts` (flat; `originIso3`/`destIso3`/`measureCode`/`value: number | null`/provenance flattened). §4.7 `packages/contracts/src/ontology/facts.ts` (`subject: SubjectRef`, `indicatorId`, `provenance: Provenance` object, `value: number` — "never null: a missing value is an ABSENT ROW"). §9.4.1 `packages/contracts/src/primitives.ts` (flat again, `destIso3: Iso3 | null` marking place-level, `value: number`). §3.8 permits `value: null`; §4.7 forbids it; §3.4 says "a missing value is an absent row". **Fix: delete two of the three. Keep §4.7's shape, make `value: number` non-nullable everywhere, and delete `value: number | null` from §3.8.**

**B5. `Provenance` has three incompatible definitions and `inheritProvenance` has two incompatible return types.** §3.8: `licenseIds: readonly string[]`. §4.7: `licenseId: string`, and the implementation returns `[...new Set(...)].join(' AND ')`. §7.4.2: a fourth shape with `sourceId` (singular), `retrievedAt`, `interval`, `placeholder`. **Fix: one interface; `licenseIds: readonly string[]` with a `formatLicenceExpression()` helper for display.**

**B6. `UNITS`/`UnitId` is exported twice from `packages/contracts` with different members.** §3.8 `snapshot-manifest.ts` exports 20 (`per_1000_pop`, `usd_2021_ppp_per_capita`, `iso_year`, `text`…). §4.6 `ontology/*` exports 11 (`per_1k_persons`, `usd_2021_ppp`, `share`…). `PERIOD_TYPES`, `ESTIMATE_KINDS` and `LATENCY_CLASSES` are likewise each declared twice in the same package. This does not compile as written, and `assertSameUnit` is meaningless across two unions. **Fix: one file, `packages/contracts/src/units.ts`, with the union stated once; §4.6 re-exports.**

**B7. `LayerSpec` has two incompatible definitions.** §6.6 (`paletteSlot`, `width?`, `dashArray?`, `opacity?: never`) vs §9.4.6 (`positions`, `indices`, `channels: Record<AuthorChannel, Float32Array>`, `pickingIds`, no `never` fields). §4.13 O8 and §9.4.6's type test assert `never` keys that §9.4.6's own interface does not declare. **Fix: keep §9.4.6's shape and add the `opacity?: never; alpha?: never; latencyClass?: never; estimateKind?: never` members to it.**

**B8. `WorldView` has two incompatible definitions and references two types that are never defined.** §5.1.3 (`country()`, `corridor()`, `assumptionSet()`, `periodGrid`, `snapshotVintage`) returns `CountryRecord | null` and `CorridorRecord | null` — neither type appears anywhere in the prompt. §9.4.5 has a different method set. **Fix: keep §9.4.5's `WorldView`, add `assumptionSet(name): AssumptionSet | null` to it, delete §5.1.3's version, and delete the two undefined record types.**

**B9. `SnapshotManifest` has two incompatible definitions and the renderer reads a field only one of them has.** §3.8: `schemaVersion: '1.0.0'` (string), extent under `manifest.cube.{corridorCount, years, attributes}`. §9.4.3: `schemaVersion: 1` (number), extent at top level as `manifest.{years, corridorCount, animatedAttributes}`. §6.5/D-53 read `manifest.cube`; §9.8 reads `manifest.corridorCount`. §4.2 and §4.6 add `manifest.json#objectTypes` and `#indicators`, which neither interface declares and `SnapshotManifestSchema` would reject. **Fix: delete §9.4.3's version; add `indicators: readonly IndicatorSpec[]` and `objectTypes: readonly (ObjectTypeSpec & {rowCount: number})[]` to §3.8's interface and its Zod mirror; state that the Zod schema is the authority and the TS interface is derived.**

**B10. `Permalink` has four incompatible shapes and two different size caps.** §4.8 `ScenarioObject`; §5.9.2 `ScenarioPermalinkV1` (cap 4,096 chars); §7.7 `ViewState` (adds `fill`, `savedViews`, `snapshotHash`; cap ~2 KB); §9.5 `Permalink` (`y`/`cam`/`layers`/`ws`/`hr`; cap ≤2 KB). Decode failure returns `Refusal('InsufficientHistory')` in §5.9.2 — semantically wrong, chosen only to avoid a sixth code — and `null` in §7.7 and §9.5. **Fix: one interface in `packages/store/src/permalink.ts` (§7.7's, it is the superset), one cap (2 KB), and `decodeView` returns `null`; delete the refusal path.**

## C. Models that cannot be computed from the specified bundle

**C1. M5 radiation is unimplementable from Tier A.** `s_ij` requires the population of every country whose centroid falls within radius `d_ij` — i.e. a full 230×230 distance matrix — but §3.1b restricts `dyads.parquet` (CEPII) to "the 12k spine corridors". It also requires `m_i`, `n_j` at the hold-out year, but `wpp_pop.parquet` is specified as "**2024** population by single-year age × sex" only. And `cepii` access is `[U]`, with the §3.2 fallback "ship without dyad context" — which silently kills M5, M5's CPC, the `/methods` scoreboard's own column and §7.3.3's drawer block, none of which name that consequence. **Fix: (a) ship a 230×230 great-circle distance matrix computed from `centroids.parquet` at build time (212 KB, no licence question) and use it for `s_ij`, reserving `cepii.dist` for the drawer's displayed value; (b) add `pop_total` by country-year 1990–2023 to `wpp_pop.parquet` and say so; (c) add to §3.2's `cepii` fallback row: "M5 is unaffected — `s_ij` uses derived centroid distances."**

**C2. M2's bisection is unimplementable from the listed WPP downloads.** §5.3.3 needs `P_b^{V7}(c,y)` — population by band under the **zero-migration** variant, 2024–2050. The five files named in §3.1a are `Demographic_Indicators_Medium`, `…_OtherVariants`, `Population1JanuaryBySingleAgeSex_**Medium**_2024-2100`, `Fertility_by_Age1`, `Life_Table_Complete_**Medium**`. There is no single-age or banded population file for VarID 7. **Fix: name the WPP file that carries single-age population by variant (and its byte count), or declare M2's bisection refused and ship only the precomputed differential.**

**C3. `M4` cannot be computed because `M` is never defined.** `C = 0.4·(m/M) + 0.3·exp(−Δt/5) + 0.3·f_obs` where `M` is "measures requested". §7.3.2 shows twelve `/place` rows; §4.11 says "the 12 registry indicators"; only five (`kStock`) and `physicians_per_1k` are ever named. **Fix: enumerate all twelve `IndicatorSpec` ids in §4.6 with their `sourceId` and `measureCodeAtSource`, and state `M = 12`.** Without this, `/place`, `/headroom`, M4, arc opacity and the whole "dimness means nobody counted" thesis are unbuildable.

**C4. `M4`'s `Δt` and `f_obs` are unspecified in ways that break purity and invert the result.** `Δt` is "the age in years of the newest vintage" — age relative to what? A wall clock breaks determinism and `snapshot:build` reproducibility. `f_obs` is "the share of that country's rows with `estimate_kind ∈ {observed, observed_flagged}`" — over which rows? If the 408k modelled spine rows count, every country's `f_obs` ≈ 0 and §4.10's worked `observedShare: 0.83` for DEU is impossible. **Fix: "`Δt = (manifest.builtAt − newest place-subject vintage) / 365.25`; `f_obs` is computed over place-subject facts only, excluding every corridor-subject row."**

**C5. M1's `H_k = ((r_k/t_k) − 1)·P/T` never says which `P`.** Which table, which year, which measure code. §3.4's dimensionless-ratio rule depends on it. **Fix: "`P` is `place_fact[measure_code='population_total', year = max(manifest.cube.years)]`, in `persons`."**

**C6. §5.4.4's three assumption sets are "median of reporting OECD / EU-27 countries" but no OECD-38 or EU-27 membership list ships**, and §9.6's own trap drops WDI aggregate rows, so the groupings cannot be recovered from the data. **Fix: commit `packages/kernel/assumptions/groups.ts` with both ISO3 lists as `as const` arrays and name it in §5.4.4.**

**C7. `K_STOCK` is spelled two ways and its cardinality is asserted as a build failure that the same prompt schedules to break.** §4.10: `physicians_per_1k, nurses_midwives_per_1k, hospital_beds_per_1k, health_spend_per_capita, dwellings_per_1k`. §5.4.3: `physicians, nurses_midwives, hospital_beds, dwellings, health_spend_pc`. §4.13 O9 asserts "exactly five `IndicatorSpec`s have `kStock: true`" while §5.4.3 instructs dropping `dwellings` at M1 if no series resolves, leaving four. **Fix: use §4.10's ids everywhere; change O9 to "four or five, and exactly four only when `DECISIONS.md` records the `dwellings` drop."**

**C8. `latencyDays` is a mandatory NOT NULL column with no definition.** Nothing says what "now" it is measured against; §9.6's reference connector hardcodes `365`. Computed against a wall clock it breaks §3.3's byte-reproducibility requirement. **Fix: "`latency_days = floor((manifest.builtAt − vintage_date) / 86400000)`, computed once in `snapshot:build`, never in a connector."**

**C9. §5.3.6's golden test is specified two ways.** §5.3.6: "a fixture-equality test over the rendered values, **not** a reproduction by `M2`… an agent that 'calibrates' `M2` until it matches has fabricated a result." §9.10: `wpp-table8-scenario-v.json` with "±0.1M absolute" tolerance, listed as a model golden vector. A tolerance implies a computation. **Fix: rename it `wp160-reference-fixture.json`, state it is a render fixture with exact string equality, and remove it from the golden-vector table.**

## D. Underspecified plumbing that will stall the agent

**D1. Connectors must resolve codes through `resolvePlace()` but are architecturally forbidden from reaching it, and `transform()` has no parameter to receive it.** §3.4/§4.12 step 3: "`transform()` … resolves every code through `resolvePlace(ns, code, onDate)`". §4.9 puts `resolvePlace` in `packages/semantic`. §9.3 permits connectors to import only `contracts` and `sdk`; `check:dogfood` fails otherwise. §9.4.4 types it `transform(raw: RawArtifact): readonly Fact[]` — no context object. And the resolver reads `snapshot/xwalk.parquet`, i.e. I/O, inside a function declared pure. **Fix: `transform(raw: RawArtifact, ctx: TransformCtx): readonly Fact[]` where `TransformCtx = { resolvePlace(ns, code, onDate): string | null }`, the crosswalk is loaded by `apps/cli` and injected, and `resolvePlace`'s pure core moves to `@exodus/sdk`.**

**D2. `check:policy`'s six rule modules are never enumerated, and the numbering contradicts itself across four sections.** §3.3 calls the install-script gate "rule module 6"; §4.4 calls the cohort cross-tab gate "rule module 6"; §7.12 calls the geometry guard "rule 6"; §10.1 D7 calls banned visual motifs "rule module 4" while §4's opening calls module 4 the `entity → place|indicator` rename. Module 5 is never named at all. **Fix: one table in §9 or §10: modules 1–6 by name (lexicon, banned output strings, banned schema fields, banned identifiers & renames & motifs, dependency/install-script manifest gate, geometry & granularity guard), each with the surfaces it walks and the path of its seeded-violation fixture.**

**D3. The `<Figure>` AST rule is the product's central gate and its implementation is unspecified.** §7.4.2 requires it to target "JSX expression children whose **inferred type** is `number`" — that needs a TypeScript program and a type checker, not the "one file walk" §2 D-52 and §9.9 describe. No library is named, and neither `ts-morph` nor the `typescript` compiler API appears in the approved dependency list, which §11.8 R9 makes a hard gate. **Fix: name the implementation ("a `ts.createProgram` over `apps/web` and `packages/ui` `tsconfig`, one `TypeChecker` pass"), add it to `ALLOWED_DEPS.json`, and state that this module runs as a separate pass from the string walk.**

**D4. Fixtures are defined as "the raw upstream bytes, verbatim" (§2.9) against a 4 MiB fixtures budget (§3.1b) for sources whose raw bytes are 145 MB and 323 MB.** §8.10 partially contradicts this for the corpus ("the committed body for GDELT is the *filtered* event rows") but says nothing about `gaskin-abel` or `ghs-h3`. **Fix: "A fixture is a bounded slice — the first N rows or N bytes sufficient to exercise `transform()` and every declared trap — with the `.meta.json` sidecar recording the full artefact's `sha256` and byte count. Raw-verbatim applies only to artefacts under 2 MiB."**

**D5. Consequently, CI's `snapshot` job measures the wrong thing.** §9.11 runs `snapshot:build --from-fixtures && snapshot:measure` and gates on "total ≤ 40 MiB". `--from-fixtures` produces the 2 MiB demo slice (§3.3). Measuring a 2 MiB slice against a 40 MiB cap proves nothing, and M1.2's "rebuilds the whole bundle" from fixtures is false. **Fix: `snapshot:measure` runs over the committed `snapshot/` tree, not over the fixture rebuild; the fixture rebuild is gated only on determinism (identical sha256 across two runs).**

**D6. Byte-reproducibility is asserted over a manifest containing a timestamp.** §3.3 "Running `snapshot:build --from-fixtures` twice must produce identical sha256 for every table"; M1.2 widens it to "byte-identical across two runs" while `manifest.builtAt` and `builderCommit` exist. Nothing pins `@duckdb/node-api`'s Parquet writer metadata (`created_by` embeds a version string) or row-group boundaries. **Fix: scope the assertion to `tables[].sha256`; require `EXODUS_CLOCK` to set `builtAt` in CI; state the writer settings (`ROW_GROUP_SIZE 65536`, SNAPPY, key-value metadata stripped).**

**D7. Every version pin is exact, several do not exist as published packages at the stated cutoff, and only four fallbacks are pre-decided.** §2 rule 3 says "apply the pre-decided fallback **where one exists**" — one exists for the spine, the corridor count, the size ladder and TanStack Router. None exists for TypeScript 7.0.2, pnpm 12.4.2, Vite 8.3.0, Turborepo 2.11.2, Biome 2.5.14, Vitest 5.0.1, Playwright 1.63.0, deck.gl 9.4.0, `@luma.gl/*` 9.4.1, `@loaders.gl/*` 4.5.1, `apache-arrow` 21.2.0, `@duckdb/node-api@1.5.5-r.5`, `size-limit@14.0.0`, `radix-ui@1.6.7`, `lucide-react@1.47.0`. `pnpm i` failing on any one of these stalls M0, which §1.1 calls the only unrecoverable failure. **Fix: add a §2 rule 4: "If a pinned version is unpublished, install the highest published version with the same major (and the same minor for deck.gl/luma.gl, which must move in lockstep), record the substitution in `DECISIONS.md` with the registry response, re-run the M0 smoke tests, and continue."**

**D8. Tailwind is IN and OUT.** D-12: "**Tailwind CSS 4.3.3** is permitted as a utility layer"; `TOOLCHAIN` pins it; the catalog does not list it. §9.9: "Do **not** install: … Tailwind. Styling is CSS Modules over the token set." §7.9 uses only CSS custom properties. **Fix: delete Tailwind from D-12 and from `TOOLCHAIN`; keep CSS Modules + tokens.**

**D9. ESLint is IN and OUT.** D-13 mandates "one **ESLint** config … a `no-restricted-syntax` rule banning `Date.now()`"; §5.1, §7, §11.8 R15 repeat it. §9.9: "Do **not** install: … ESLint"; §9.10 replaces it with `tools/grit/no-clock.grit`. **Fix: delete ESLint from D-13 and rewrite §5.1's and R15's enforcement column as "Biome GritQL plugin `tools/grit/no-clock.grit`".**

**D10. Vite version contradiction.** D-08/`TOOLCHAIN`/catalog say 8.3.0; §6.14 item 7 gates on "the `visgl:webgl-only` export condition resolves under **Vite 7**". **Fix: 8.3.0.**

**D11. M7.4 contradicts D-38a.** D-38a: "**The acceptance test asserts the landing URL, not the status code**" because a static host cannot emit a 301 from the bundle. M7.4: "Status code 301 asserted, not 302." **Fix: M7.4 reads "the landing URL after navigation is `/methods#allocation`; no status code is asserted."**

**D12. `pnpm dev` is never told how to serve `snapshot/`.** §9.13 covers the build only. 33 MiB of Parquet through Vite's transform pipeline is a stall. **Fix: "`apps/web/vite.config.ts` sets `publicDir: '../../snapshot'` served at `/snapshot/`, with `assetsInclude: ['**/*.parquet']` and `optimizeDeps.exclude` for the loaders; the loader's base path is `${import.meta.env.BASE_URL}snapshot/`."**

**D13. `deck.needsRedraw()` has a clearing side effect and the prompt tells you to call it in your own idle detector.** §2 D-36 and §9.11: "`__deckIdle` … you set it yourself from `onAfterRender` when `deck.needsRedraw()` is false." Calling it without options consumes deck's redraw flags and will desynchronise its own loop. **Fix: "`deck.needsRedraw({clearRedrawFlags: false})`."** Related: GLOBE-04 ("idle canvas issues ≤2 renders in 3s") never says how the animation loop is quiesced. **Fix: state `_animate: false` and that layer props are pushed via `deck.setProps`, not a continuous loop.**

**D14. Turborepo cannot cache the `ingest` task as declared.** §9.9: `"ingest": { "outputs": ["../../.cache/raw/**"] }` — outputs outside the package directory are not cacheable. **Fix: outputs go to `<package>/.ingest-out/**` and `snapshot:build` reads from the workspace glob.**

**D15. Three names for the gitignored output root.** §3.3: `raw/` and `.recipe-out/`; §9.1: `.cache/` holding "raw fetch output, **recipe-connector output**" — merging the two directories the licence gate depends on keeping apart. **Fix: `.cache/raw/<id>/` and `.cache/recipe-out/<id>/`, both gitignored, and `snapshot:build` refuses to read anything under `recipe-out`.**

**D16. Two corpus roots.** §3.3 puts it at `snapshot/corpus/{sourceId}/`; §8.10 and §9.1 put it at repo-root `corpus/`; §3.7 says `bundle:verify` "runs over `snapshot/` and `corpus/`". **Fix: `snapshot/corpus/` only.**

**D17. `xwalk` has two authoritative homes.** §3.1a: committed source at `packages/connectors/xwalk/table.csv`; §4.12 step 5 instructs adding rows directly to `snapshot/xwalk.parquet`, a build artefact. **Fix: the CSV is the source; step 5 reads "add rows to `packages/connectors/xwalk/table.csv` and re-run `snapshot:build`."**

**D18. Tier B is referenced but never specified.** §3.1b, §3.3 `pnpm data:full` "checksum-verified". No release tag scheme, no asset name, no checksum sidecar path, no failure behaviour, and no statement of who publishes it — the agent has no release. **Fix: either cut Tier B and `pnpm data:full` entirely (the product is complete without it, per §3.3), or specify `tag: data-v<manifest.schemaVersion>-<YYYYMMDD>`, asset `flows-full.parquet` + `flows-full.parquet.sha256`, and "on checksum mismatch, delete the download and exit 1."**

**D19. Scripts are declared in four places and no list is complete.** `pnpm verify:sources` (§3.2, D1), `pnpm fixtures:synth` (§3.2), `pnpm gen:parameters` (§5.1.3), `pnpm live` (§8), `pnpm golden:accept` (§9.9), `pnpm gate:m0..m9` / `gate:all` (§11.1) — §9.9's table is missing four of them, §3.3's table is missing five. `pnpm gate:m0 --check spine-resolve` uses a sub-flag that is never defined. **Fix: one script table in §9.9 covering every command any section invokes, and a `--check <id>` definition in §11.1's `runGate`.**

**D20. The `fixtures:synth` escape hatch has no enforcement point.** §3.2: "`bundle:verify` refuses to mark the bundle shippable." There is no shippable flag; `manifest.synthetic: boolean` exists and `SnapshotManifestSchema` places no constraint on it, and no `bundle:verify` gate in §3.7 reads it. **Fix: add gate 13: "`manifest.synthetic === true` exits non-zero unless `--allow-synthetic` is passed," and add a `/`-route non-dismissible banner test.**

**D21. §11.12's first-five-commands block cannot produce `GA_URL`.** Command 2 is `curl -sSIL` on the DOI (headers only); command 3 says "set `GA_URL` to the file URL for `mig_bilateral.csv` as shown on the record resolved in step 2." §3.1a specifies the correct route — `https://zenodo.org/api/records/17344747` → `files[].links.self` where `key == "mig_bilateral.csv"`. The command block omits it. **Fix: insert between commands 2 and 3: `curl -sS https://zenodo.org/api/records/17344747 | tee docs/smoke/m0-spine-files.json | jq -r '.files[] | select(.key=="mig_bilateral.csv") | .links.self'`** — and add `jq` to the assumed tooling, or give a `node -e` equivalent.

**D22. Nothing tells the agent what is in the checkout it was handed.** §1.6 describes a rename and deletes two probe files (`jv.json`, `est.json`) that do not exist in this repository. The actual checkout is `docs/{research,design,prompt}` and nothing else; `git status --short` is already clean. **Fix: replace §1.6 step 5 with a statement of the actual contents and an instruction ("the checkout contains only `docs/**`; everything else you create"), because as written the agent will hunt for files that are not there.**

**D23. No effort or time budget exists, yet the build is gated on one.** §11.7: M9 "only if … `pnpm gate:all` is green **with time left over**"; §1.9 and §11.1 refer to "a budget that ends early". **Fix: state the budget in tokens or wall-clock, or make M9's gate mechanical ("M9 is attempted only if `m8-green` is tagged and no `DECISIONS.md` entry records an applied cut-ladder rung").**

## E. Render and UI gaps

**E1. Two different alpha mappings, two different dash mappings, two different "not computable" treatments.** Opacity: §6.6 `round(255·(0.15+0.85·C))` vs §7.9.5 `0.12 + 0.68·C`. Dash: §6.6's continuous `[max(2, 12−10·min(d,1)), 2+10·min(d,1)]` vs §7.9.5's five-bucket ordinal table vs §7.3.1's three-category legend. Not-computable: §6.6 `[3,3]` vs §7.9.5 `1px dotted [1,14] at 0.35× alpha`. **Fix: keep §7.9.5's bucketed table (it is the one the legend can render), delete §6.6's formulas, and make §7.3.1's legend enumerate the five buckets plus the not-computable entry.**

**E2. The default camera is specified three ways.** §2.9 "longitude 20, latitude 5"; §6.2 `{longitude: 25, latitude: 5, zoom: 0.55}`; §7.3.1 `{longitude: 20, latitude: 8, zoom: 0.4}`. CAM-01 gates on six centroids in frame. **Fix: "`packages/globe/src/camera.ts` is the only place `DEFAULT_CAMERA` exists; its committed value is whatever passes CAM-01 at M2. No other section states a literal."**

**E3. The `incidents` layer's data source is self-contradictory and one reading violates E2/E3 of §10.1.** §6.3: `ScatterplotLayer`, ≤3,000 points — requires coordinates. §6.4 feeds it `snapshot/corpus-index.json`, a file with no spec anywhere. §3.3 names `incidents.parquet` as "iom-mm, ADM0 annual counts, **no coordinates**", and §10.1 E3 forbids `iom-mm` binding to any map layer. **Fix: rename `incidents.parquet` → `iom_mm_annual.parquet`; state the `incidents` **layer** reads `snapshot/corpus/index.json` (GDELT/GDACS/USGS `WorldEvent` points only) and give that file a schema; add the §8.4 conditional ("rendered only at the final year frame") to §6.3's table so the frame budget is honest.**

**E4. Export exists and does not exist.** §7.7 specifies PNG/SVG export with a burned-in stamp, §7.4 exports `<ExportStamp>`, §7.4.1's action list includes `export png`/`export svg`. §10.9.1: "**There is no export or download feature in v1**, so no export stamp ships." **Fix: pick one. If export ships, §10.9.1's stamp copy becomes normative and `<ExportStamp>` renders it; if not, delete `<ExportStamp>`, the two palette actions and §7.7's export block.**

**E5. `localStorage` is permitted and banned.** D-29 permits it for the `saved_views` tray; §7.3.7 names the key `corridor.saved_views.v1`; §7.13.1 adds `corridor.firstrun.v1` and §7.13.3 `corridor.tour.v1`. §8.9 and §9.15: "no `localStorage` beyond the once-per-session entry-animation flag in `sessionStorage`" — which is itself incoherent. **Fix: §9.15 reads "`localStorage` is permitted for exactly three keys — `corridor.saved_views.v1`, `corridor.firstrun.v1`, `corridor.tour.v1` — each read and written inside try/catch; `sessionStorage` for `corridor.entry.v1` and `corridor.tier.v1`; nothing else."**

**E6. Two "exact copy" first-run notices and two "exact" footer strings.** §7.13.2 vs §10.9.1 give different verbatim first-run panels. §7.2's footer ("Estimates with **uncertainty** · vintages vary…") vs §10.9.1's ("Estimates, not counts · vintages differ by source…"). The §7.2 string contains "uncertainty" in permanent chrome, adjacent to every disagreement figure on screen — a U1 violation that fails `check:policy`. **Fix: §10.9.1's strings are normative for both; delete §7.2's and §7.13.2's.**

**E7. Four overlays, five overlays.** §7.3.7 and §7.14.1 fix the count at four (palette, `Shift+G`, provenance popover, `saved_views`); §7.13.3 adds a guided tour with its own anchors and `aria-live` announcements; §7.10 adds a policy-verdict sheet and §7.2's z-order lists seven layers including "refusal/policy sheet" and "first-run notice". **Fix: state the overlay set once (six), and correct §7.14.1's axe-run count.** Also: §7.4.1 says the `>` actions "are exactly" ten and omits `take the tour`, which §7.13.3 requires.

**E8. Arrow keys are bound to two things.** §6.5/§7.5: arrows step the year ±1. §7.11.3: "Arrow keys move a country cursor through a spatially-sorted list of the 258 ADM0 features." **Fix: state focus scoping — "arrows step the year only when focus is inside the Year Machine region (F6 region 5); inside the canvas region they move the country cursor."**

**E9. `Shift+G` has three mutually exclusive mountings.** §7.3.7 an overlay; §7.11.3 "always present (visually collapsed, never `display:none`)" as the canvas's `aria-describedby` target; §6.12 FALLBACK renders it as the page's primary content. **Fix: one component, always mounted, `aria-describedby` target at all times, visually hidden by default, promoted to a dialog by `Shift+G` and to primary content under FALLBACK — state this once in §7.11.4.**

**E10. `--text-muted` (4.44:1 dark / 4.41:1 light) is declared "labels and axes only, **never body**" and is then assigned to the permanent footer strip's body copy** (§7.2: "11px, `--text-muted`"). **Fix: footer copy uses `--text-secondary`.**

**E11. The ocean sphere is "mandatory" and has no construction spec.** §6.3/§6.4 say one polygon ring `[[-180,-90],[180,-90],[180,90],[-180,90]]` generated in code. Its altitude/radius is unstated, and it is the only depth-writing layer; corridors ride at 8,000–180,000 m with `depthCompare: 'less-equal'`. Nothing says the sphere sits at altitude 0, nor what tessellation density `SolidPolygonLayer` needs for the ring to project as a sphere rather than four great-circle edges. **Fix: "the ring is densified to a vertex every 2° before construction, `getElevation: 0`, `_normalize: false`," plus a screenshot test at two antipodal cameras.**

**E12. Natural Earth simplification is budgeted but not specified.** §3.1b: "258 ADM0 polygons **simplified for the globe**, shipped as gzipped GeoJSON" at 3 MiB, fed as plain objects to a `SolidPolygonLayer` (§6.4, "accessors, not binary"). No tolerance, no tool, no vertex budget, no MultiPolygon/hole handling, and the `countries` layer's 0.6 ms / 2.4 ms budget depends entirely on the vertex count. **Fix: "simplified with `mapshaper -simplify visvalingam 8% keep-shapes`, target ≤120,000 total vertices, asserted by a build-time test; MultiPolygons are flattened to one `SolidPolygonLayer` datum per ring with holes preserved."**

**E13. Fonts must be self-hosted, subset, licensed and under 160 KiB, and the prompt never says where they come from or how to subset them.** §7.9.2. `fonttools`/`pyftsubset` is not in the approved dependency list, which R9 makes a hard gate. **Fix: name the download URLs, the subsetting command, the unicode ranges, the SIL OFL file paths in `LICENSES/`, and add the tool to `ALLOWED_DEPS.json`.**

**E14. The tier probe is circular.** §6.12: the probe runs during the 2.4s entry move, or "a hidden 30-frame probe at load"; its output selects `corridorCount`, which determines the cube build, which must complete before the first frame (§7.13.1: nothing pops in). **Fix: "the tier probe runs against the ocean sphere + graticule only, before the cube is built; `corridorCount` is applied as a prefix length when the cube's buffers are first bound."**

**E15. `EXODUS_CORRIDOR_LIMIT` is the documented M0 escape hatch and cannot reach the browser.** §9.8 rule 4 inlines exactly three vars into the client bundle and this is not one of them; nor is `EXODUS_CLOCK`, which §9.8's own table says "freezes … the AS-OF chip". **Fix: add both to the inlined set.** Also `?tier=LOW` (§6.12) is an un-validated search param under D-09's Zod rule — **add it to the route's search schema.**

**E16. The AttributeCube build has no time budget and no thread.** 384,000 great-circle vertices plus 34×12,000×3 quantisations plus 102 GPU buffer uploads, all inside the ≤3s cold-boot target (§7.10) on the main thread. **Fix: state a budget (e.g. ≤600 ms), state whether it runs in a worker, and add it as a measured M2 gate with a pre-decided fallback (build the current year's frame first, fill the remaining 33 during idle).** Relatedly, `buildAttributeCube(facts: readonly Fact[], …)` materialising ~400k JS objects contradicts the binary-path thesis — **change the signature to take Arrow `Table`s.**

## F. Process and documentation gaps

**F1. Three different closed lists of permitted documents.** §1.8: `DECISIONS.md, SNAPSHOT.md, PARAMETERS.md, BACKLOG.md, README.md, ETHICS.md, REUSE.toml`. §11.8 R12: four + `REUSE.toml` (omits README and ETHICS). §9.16: those plus `CONTRIBUTING.md, SECURITY.md, GOVERNANCE.md, CITATION.cff, LICENSES/, docs/smoke/, compliance/ai-act/*.md, packages/*/README.md`. §9.16 also says a `check:policy` docs rule "fails the build on any `.md` file outside this list" — under R12's list that rule fails on `README.md`. **Fix: one list, in §9.16, and R12 cites it by reference.**

**F2. No milestone owns the compliance artefacts.** `ETHICS.md` (§10.10, fourteen sections), `GOVERNANCE.md`, `CONTRIBUTING.md`, `SECURITY.md`, `CITATION.cff`, `REUSE.toml`, `harm-report.yml`, one `DatasetDatasheet` per source (§10.8, `check:policy` fails without them), five `ModelCard`s, and `NOTICE` generation. §10.13 says the *tests* exist "before M7"; §11's M0–M8 deliverable lists name none of the artefacts. **Fix: assign each to a milestone in §11 — datasheets to M4 (they are `/sources` content), model cards to M7, `ETHICS.md`/`GOVERNANCE.md`/`REUSE.toml`/harm-report to M7, `CITATION.cff`/`CONTRIBUTING.md`/`SECURITY.md` to M8 — and add their checks to the corresponding gate tables.**

**F3. `ModelCard.assumptions[].placeholder` is typed `false` (literal) in §10.8 and `boolean` in §9.4.5 `AssumptionSpec`.** They are the same field rendered on the same page. **Fix: `AssumptionSpec.placeholder: boolean` in the contract; `ModelCard` narrows to `false` via a branded `ShippedAssumption = AssumptionSpec & {placeholder: false}`.**

**F4. The suppression ledger's append-only test cannot run in CI as configured.** §3.6/§10.13: "replaying the ledger's prior state from git history"; §9.11 uses `actions/checkout` with no `fetch-depth`, which defaults to 1. **Fix: `fetch-depth: 0` on the `snapshot` job, and the test compares against `git merge-base origin/main HEAD`.**

**F5. Complementary suppression is undefined for the tables it runs on.** §3.6: "in any **margin** with exactly one suppressed cell, suppress the next-smallest cell in that margin." For a `(orig, dest, year)` table there are at least three candidate margins and the prompt names none. Nor does it say which tables suppression runs over (the spine is modelled output, not counts), nor the expected magnitude of `manifest.suppression.suppressedCells`. **Fix: "suppression runs over `flows.parquet` and `asylum.parquet` only. Margins are `(orig, year)` and `(dest, year)`. Both are processed, origin margin first, to a fixed point."**

**F6. `bundle:verify` gate 10 is dead and gate 2 makes GDACS unshippable.** Gate 10: "No file under `snapshot/` exceeds 50 MiB, and the total is at or under 40 MiB" — the 50 MiB clause can never fire. Gate 2's allowlist has no entry for GDACS, and §8.12 assigns it `redistributable: false`, which gate 1 refuses — so GDACS can never be committed, yet §3.1b budgets the 6 MiB corpus with it and §8.6 lists it as one of three sources. **Fix: delete the 50 MiB clause; state in §3.1b that the corpus budget assumes GDELT + USGS only, with GDACS added on written licence resolution.**

**F7. The Docker offline stage cannot install.** §9.13: `RUN corepack enable && pnpm install --frozen-lockfile --offline` — `--offline` requires a populated pnpm store, and the Dockerfile copies none. **Fix: add a network-enabled install stage that primes `/pnpm-store`, `COPY --from` it into the build stage, and set `store-dir` in `.npmrc`.**

**F8. Visual-regression baselines have no generation procedure.** §9.11 gates `toHaveScreenshot({maxDiffPixelRatio: 0.01})` on SwiftShader with FXAA + vignette, across runners, with no image digest pinned and no `--update-snapshots` policy. This fails on the first CI run after any base-image change. **Fix: pin the Playwright container image by digest for the `visual` job, and state that baselines are generated inside that image and committed, never updated by a maintainer's local run.**

**F9. `REUSE.toml` cannot express a mixed-licence Parquet file.** `capacity.parquet` mixes `wdi` (CC-BY-4.0) and `who-gho` (contested) rows; REUSE is per-file. **Fix: state the per-file SPDX expression form (`CC-BY-4.0 AND LicenseRef-WHO-GHO-unverified`) and require `snapshot:build` to emit the `REUSE.toml` entries from `TableEntry.sourceIds` rather than hand-authoring them.**

**F10. `M8.2`'s zero-diff assertion will fail on the lockfile.** `pnpm gen:connector` creates a new workspace package, which changes `pnpm-lock.yaml`. The filter in §9.6 permits only `packages/connectors/demo-wdi-gdp/` and `snapshot/`. Also unspecified: what `gen:connector` does when the target directory already exists (§9.1 commits `demo-wdi-gdp/`, and CI regenerates it). **Fix: add `pnpm-lock.yaml` to the permitted set; state that `gen:connector` refuses a non-empty target unless `--force`, and that CI runs it into a temp id (`demo-wdi-gdp-ci`) which it then deletes.**

**F11. The performance ladder has no rung that can save the frame budget it is meant to save.** §6.11's SwiftShader column sums to −0.9 ms slack before any measurement, and the only ladder (§6.11, M0.4, §9.12) reduces corridors 12,000 → 6,000 → 4,000, touching 5.5 ms of a 19.1 ms budget while `h3-density` (6.0 ms) and `countries` (2.4 ms) are untouched. There is no rung below 4,000 and no stated behaviour if 4,000 still fails. **Fix: extend the ladder — "5. H3 r3 → r2 (41,162 → 5,882 cells); 6. drop the `vignette` pass; 7. `useDevicePixels: false`" — and state the terminal behaviour: "if rung 7 still misses, record the measured median in `SNAPSHOT.md` and on `/methods`, mark PERF-01 non-blocking in `DECISIONS.md`, and continue."**

**F12. The CPC hold-out year is 2010 in one place and 2022 in another.** §5.6.2 fixes it at 2010 and explains why any year inside 2015–2023 leaks corridor selection into the evaluation. §7.3.3's drawer mock reads "Radiation CPC (held-out **2022**) 0.14". **Fix: 2010 in §7.3.3.**

**F13. Nothing defines what happens for a corridor outside the shipped 12,000.** §3.3 says such a lookup "returns `Refusal('NoSharedPeriodGrid')` or a `—`" — the wrong code; that one means "outside the shared period grid", which is a different fact about a different axis. Rendering it would tell the user a true statement about the wrong thing. **Fix: define the `/corridor/:orig-:dest` behaviour for a non-spine pair explicitly — either a route-level 404 non-ideal state naming the 12,000-corridor bundle, or an eighth/ninth `RefusalCode` — and update §5.1's closed list accordingly.**

**F14. `/place/:iso3` has no defined behaviour for an unknown or historic ISO3.** §7.10's error table covers "unknown route" only, while `Place.validFrom/validTo/successorIds` explicitly model SUN, YUG, SCG and pre-2011 SDN as real rows. **Fix: "an ISO3 absent from `places` renders the 404 non-ideal state; an ISO3 whose `validTo` is non-null renders the page with a successor banner linking each `successorIds` entry."**