# SPDX-FileCopyrightText: Exodus contributors
# SPDX-License-Identifier: AGPL-3.0-or-later
name: scorecard
on:
  branch_protection_rule:
  schedule: [{ cron: '27 4 * * 1' }]
  push: { branches: ["main"] }
permissions: read-all

jobs:
  analysis:
    name: OpenSSF Scorecard
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      # Actions are pinned to a commit SHA, not a floating tag: Scorecard's own
      # Pinned-Dependencies check marks floating tags down, and a tag can be moved.
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with: { persist-credentials: false }
      - uses: ossf/scorecard-action@62b2cac7ed8198b15735ed49ab1e5cf35480ba46 # v2.4.0
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true
      - uses: github/codeql-action/upload-sarif@1b549b9259bda1cb5ddde3b41741a82a2d15a841 # v3.28.13
        with: { sarif_file: results.sarif }
